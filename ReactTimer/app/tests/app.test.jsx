var expect = require('expect');

// visual test function // formats your output to be more readable
describe('App', () => {
    it('should properly run tests', () => {
        expect(1).toBe(1);
    });
});